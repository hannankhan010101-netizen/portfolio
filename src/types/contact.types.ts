/**
 * Values collected by the contact form before submission.
 */
export interface ContactFormValues {
  readonly name: string;
  readonly email: string;
  readonly subject: string;
  readonly message: string;
}
