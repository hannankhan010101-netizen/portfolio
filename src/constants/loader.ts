/**
 * Site-wide splash loader timing (non-secret UX tuning).
 * Kept short so the shell dismisses soon after DOM is interactive (not after every asset).
 */
export const SITE_LOADER_MIN_MS = 380;

export const SITE_LOADER_SUBTITLE = "Software Engineer II · Hatzs Dimensions";
