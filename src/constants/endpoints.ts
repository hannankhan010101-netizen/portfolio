/**
 * Relative API paths used with the shared HTTP client.
 * Do not duplicate these strings in service files; import from here.
 */
export const ENDPOINTS = {
  CONTACT: "/api/contact",
} as const;
