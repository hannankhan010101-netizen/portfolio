/**
 * Default sender when using Resend's onboarding domain (non-secret).
 * Override with RESEND_FROM_EMAIL in production after domain verification.
 */
export const RESEND_DEFAULT_FROM = "Portfolio <onboarding@resend.dev>";
